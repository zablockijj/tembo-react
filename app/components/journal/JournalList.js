import React from 'react'

export default React.createClass({
    render() {

        // var journalNodes = this.props.data.map(journal => {
        //     return (
        //         <li><JournalLink to="/journal">{journal.title}</JournalLink></li>
        //     );
        // });
        return (
            <ul>test</ul>
        )
    }
})
